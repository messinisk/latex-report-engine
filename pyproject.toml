[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "report_engine"
version = "0.1.0"
description = "A modular LaTeX reporting engine"
authors = [
    { name="Kostas" }
]
dependencies = []
