name: SLSA Provenance

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  id-token: write
  contents: write

jobs:
  build:
    uses: slsa-framework/slsa-github-generator/.github/workflows/slsa.yml@v1.9.0
    secrets: inherit
